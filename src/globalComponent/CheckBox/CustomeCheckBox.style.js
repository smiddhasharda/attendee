import { StyleSheet } from 'react-native';

const CustomeCheckBoxStyle = StyleSheet.create({
  container : { flexDirection: 'row', alignItems: 'center' },
  body : {
    width: 20,
    height: 20,
    borderWidth: 1,
    borderColor: 'black',
  },
  text:{ marginLeft: 8 }
});

export default CustomeCheckBoxStyle;
